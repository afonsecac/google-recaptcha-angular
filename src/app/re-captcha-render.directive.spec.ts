import { ReCaptchaRenderDirective } from './re-captcha-render.directive';

describe('ReCaptchaRenderDirective', () => {
  it('should create an instance', () => {
    const directive = new ReCaptchaRenderDirective();
    expect(directive).toBeTruthy();
  });
});
